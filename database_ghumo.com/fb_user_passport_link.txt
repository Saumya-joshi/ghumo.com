CREATE TABLE fb_user_passport_link(user_id int, passport_no varchar(20), PRIMARY key(user_id, passport_no), FOREIGN key(user_id) REFERENCES fb_users(id), FOREIGN key (passport_no) REFERENCES passport_details(passport_no));